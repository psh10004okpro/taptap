import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taptap.titans',
  appName: 'TapTap Titans',
  webDir: 'dist',
  android: {
    backgroundColor: '#0d0a1a',
  },
};

export default config;
